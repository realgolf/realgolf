[build]
  command = "npm run build"  # oder der Befehl, um den SvelteKit-Build durchzuführen
  publish = "site/en/offline/build"  # Verzeichnis, in dem der SvelteKit-Build liegt
  base = "site/en/offline"  # Basisverzeichnis für den Build-Prozess